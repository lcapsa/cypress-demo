export default class EventDetails {
  constructor(name, description, inviteMode, startDate, endDate) {
    (this.name = name),
      (this.description = description),
      (this.inviteMode = inviteMode),
      (this.startDate = startDate),
      (this.endDate = endDate);
  }
}
