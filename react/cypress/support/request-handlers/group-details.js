export default class GroupDetails {
  constructor(name, isPublic, description, cover) {
    (this.name = name),
      (this.isPublic = isPublic),
      (this.description = description),
      (this.cover = cover);
  }
}
